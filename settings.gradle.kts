pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "Food2ForkKMM"
include(":androidFood2ForkKMM")
include(":shared")